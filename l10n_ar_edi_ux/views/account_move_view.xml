<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_move_form" model="ir.ui.view">
        <field name="name">account.move.form</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="l10n_ar_edi.view_move_form_ar"/>
        <field name="arch" type="xml">
            <field name="l10n_ar_afip_fce_is_cancellation" position="after">
                <label for="l10n_ar_afip_asoc_period_start" string="Associated Period"/>
                <div>
                    <field name="l10n_ar_afip_asoc_period_start" class="oe_inline"/> to <field name="l10n_ar_afip_asoc_period_end" class="oe_inline"/>
                </div>
                <field name='l10n_ar_boarding_permission_ids' widget="many2many_tags" invisible="l10n_ar_afip_ws != 'wsfex'" options="{'no_quick_create': True}"/>
                <button name="check_valid_boarding_permission" string="Validar permisos de embarque" type="object" invisible="l10n_ar_afip_ws != 'wsfex'" icon="fa-arrow-right" class="oe_link" colspan="2"/>
            </field>

            <!-- Cambiamos comportamiento de Odoo, movemos el campo Resultado ARCA a la pestaña ARCA como estaba previo a este cambio https://github.com/odoo/enterprise/commit/792907aad38ad283f923680222afe2b51ea5f0dd -->
            <xpath expr="//page[@name='afip']/group/label" position="before">
                <field name="l10n_ar_afip_result" position="move"/>
            </xpath>

            <!-- Mostrar datos relacionados a la verificacion de CAE solo si es compras y no es WS ARCA -->
            <label for="l10n_ar_afip_auth_code" position="attributes">
                <attribute name="invisible" separator=" or " add="l10n_ar_afip_ws"/>
            </label>
            <div invisible="l10n_ar_afip_verification_type == 'not_available' or move_type not in ('in_invoice', 'in_refund', 'in_receipt')" position="attributes">
                <attribute name="class">d-flex gap-2</attribute>
                <attribute name="name">ar_verify_vendor</attribute>
                <attribute name="invisible" separator=" or " add="l10n_ar_afip_ws"/>
            </div>

            <div name="ar_verify_vendor" position="after">
                <button name="l10n_ar_verify_on_afip" position="move"/>
            </div>


        </field>
    </record>

</odoo>
